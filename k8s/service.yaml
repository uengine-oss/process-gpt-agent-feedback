apiVersion: v1
kind: Service
metadata:
  name: agent-feedback
  namespace: dev
  labels:
    app: agent-feedback
spec:
  type: ClusterIP
  selector:
    app: agent-feedback
  ports:
    - name: http
      port: 6789
      targetPort: 6789
